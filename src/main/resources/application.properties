# Google Cloud Monitoring configuration
management.stackdriver.metrics.export.project-id=carbon-scene-478408-c4

# Disable the default JobLauncherApplicationRunner to avoid conflicts with our custom runner
spring.batch.job.enabled=false
